---
import { TAG_LABELS, TAG_COLORS } from "../lib/constants";

interface Props {
  tag: string;
}
const { tag } = Astro.props;
const label = TAG_LABELS[tag] ?? tag;
const color = TAG_COLORS[tag] ?? "#f3ede2";
---

<span class="nb-tag" style={`background: ${color};`}>{label}</span>
